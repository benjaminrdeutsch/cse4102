(** UConn CSE 4102 **)
(** Spring 2026 **)
(** Homework 0 **)

(* Ben Deutsch *)
let my_name = "(Ben Deutsch)"

exception Unimplemented

(* True-false questions *)
(* Static types are checked at compile time *)
let q1 = true
(* There are more than 4 language paradigms *)
let q2 = true
(* An interpreter translates a high-level program to machine code *)
let q3 = false
(* The first compiler was written by Alan Turing *)
let q4 = false
(* It's possible to use ideas from one PL in another *)
let q5 = true


(* Do not modify below *)
let ch = open_out "hw0_out"
let _ = Printf.fprintf ch "Generated by %s\n" Sys.executable_name
let _ = if !Sys.interactive then Printf.fprintf ch "(interactive)\n\n"
        else Printf.fprintf ch "(non-interactive)\n\n"
let _ = Printf.fprintf ch "Name: %s\n" my_name

let _ = Printf.fprintf ch "My answers to the T/F questions:\n%s\n"
          (String.concat "\n"
             (List.map string_of_bool [q1; q2; q3; q4; q5]))

let _ = Printf.fprintf ch "OS: %s \n" Sys.os_type
let _ = close_out ch
